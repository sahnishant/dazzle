---
// src/pages/collections/index.astro
import { Image } from 'astro:assets';
import BaseLayout from '../../layouts/BaseLayout.astro';
import CollectionGrid from '../../components/CollectionGrid.astro';
import FilterSidebar from '../../components/FilterSidebar.astro';
import { jewelryItems } from '../../data/jewelry.js';

const collections = [
  { name: 'Rings', image: '/images/collection-rings.png', link: '/collections/rings/' },
  { name: 'Necklaces', image: '/images/collection-necklaces.png', link: '/collections/necklaces/' },
  { name: 'Earrings', image: '/images/collection-earrings.png', link: '/collections/earrings/' },
  { name: 'Bracelets', image: '/images/collection-bracelets.png', link: '/collections/bracelets/' },
  { name: 'Custom Designs', image: '/images/collection-custom.webp', link: '/collections/custom/' },
];

const categories = [...new Set(jewelryItems.map((item) => item.category).filter(Boolean))].sort();
const materials = [...new Set(jewelryItems.map((item) => item.material).filter(Boolean))].sort();
const formatLabel = (value) => value.replace(/-/g, ' ').replace(/\b\w/g, (char) => char.toUpperCase());
---
<BaseLayout title="Collections" description="Browse our exquisite diamond jewelry collections by type, material, and design style.">
  <section class="container mt-4 mb-4 collections-page">
    <div class="collections-heading">
      <p class="collections-kicker">Boutique catalogue</p>
      <h1>Our Jewelry Collections</h1>
      <p>Browse by collection, or refine the full catalogue by category and material.</p>
    </div>

    <CollectionGrid collections={collections} />

    <div class="browse-section" id="browse-all-pieces">
      <div class="browse-heading">
        <p class="collections-kicker">All pieces</p>
        <h2>Browse every design</h2>
        <p id="collectionResultCount">Showing {jewelryItems.length} pieces</p>
      </div>

      <div class="browse-layout">
        <FilterSidebar categories={categories} materials={materials} />

        <div class="product-results" id="collectionProductGrid">
          {jewelryItems.map((item) => (
            <article
              class="collection-product-card"
              data-product-card
              data-category={item.category}
              data-material={item.material}
            >
              <a href={`/collections/${item.category}/${item.id}/`}>
                <Image
                  src={item.mainImage}
                  alt={item.name}
                  width={760}
                  height={600}
                  format="webp"
                  loading="lazy"
                  decoding="async"
                  class="collection-product-image"
                />
                <div class="collection-product-copy">
                  <p class="collection-product-meta">{formatLabel(item.category)} · {item.material}</p>
                  <h3>{item.name}</h3>
                  <p class="collection-product-price">{item.price}</p>
                </div>
              </a>
            </article>
          ))}
        </div>
      </div>

      <p id="emptyCollectionState" class="empty-state" hidden>No pieces match the selected filters. Clear filters to see the full catalogue.</p>
    </div>
  </section>
</BaseLayout>

<script is:inline>
  document.addEventListener('DOMContentLoaded', () => {
    const filterInputs = Array.from(document.querySelectorAll('[data-collection-filter]'));
    const cards = Array.from(document.querySelectorAll('[data-product-card]'));
    const clearButton = document.getElementById('clearCollectionFilters');
    const resultCount = document.getElementById('collectionResultCount');
    const emptyState = document.getElementById('emptyCollectionState');

    const getCheckedValues = (type) => new Set(
      filterInputs
        .filter((input) => input.dataset.collectionFilter === type && input.checked)
        .map((input) => input.value)
    );

    const applyFilters = () => {
      const selectedCategories = getCheckedValues('category');
      const selectedMaterials = getCheckedValues('material');
      let visibleCount = 0;

      cards.forEach((card) => {
        const matchesCategory = selectedCategories.size === 0 || selectedCategories.has(card.dataset.category);
        const matchesMaterial = selectedMaterials.size === 0 || selectedMaterials.has(card.dataset.material);
        const isVisible = matchesCategory && matchesMaterial;

        card.hidden = !isVisible;
        if (isVisible) visibleCount += 1;
      });

      if (resultCount) {
        resultCount.textContent = `Showing ${visibleCount} ${visibleCount === 1 ? 'piece' : 'pieces'}`;
      }

      if (emptyState) {
        emptyState.hidden = visibleCount > 0;
      }
    };

    filterInputs.forEach((input) => input.addEventListener('change', applyFilters));
    clearButton?.addEventListener('click', () => {
      filterInputs.forEach((input) => {
        input.checked = false;
      });
      applyFilters();
    });

    applyFilters();
  });
</script>

<style>
  .collections-page {
    padding-bottom: 3rem;
  }

  .collections-heading,
  .browse-heading {
    max-width: 760px;
    margin: 0 auto 2rem;
    text-align: center;
  }

  .collections-heading h1,
  .browse-heading h2 {
    margin: 0 0 0.75rem;
    color: #555;
  }

  .collections-heading p:last-child,
  .browse-heading p:last-child {
    margin: 0;
    color: #666;
    line-height: 1.65;
  }

  .collections-kicker {
    margin: 0 0 0.35rem;
    color: #A0907C;
    font-size: 0.82rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .browse-section {
    margin-top: 4rem;
  }

  .browse-layout {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
    align-items: start;
  }

  .product-results {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(230px, 1fr));
    gap: 1.4rem;
  }

  .collection-product-card {
    overflow: hidden;
    border-radius: 12px;
    background: #fff;
    box-shadow: 0 4px 14px rgba(0, 0, 0, 0.07);
    transition: transform 0.25s ease, box-shadow 0.25s ease;
  }

  .collection-product-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 10px 22px rgba(0, 0, 0, 0.1);
  }

  .collection-product-card a {
    display: flex;
    min-height: 100%;
    flex-direction: column;
    color: #333;
    text-decoration: none;
  }

  .collection-product-image {
    width: 100%;
    height: 230px;
    object-fit: cover;
  }

  .collection-product-copy {
    display: flex;
    flex: 1;
    flex-direction: column;
    padding: 1rem;
  }

  .collection-product-meta {
    margin: 0 0 0.4rem;
    color: #A0907C;
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .collection-product-copy h3 {
    margin: 0 0 0.75rem;
    color: #555;
    font-size: 1.1rem;
    line-height: 1.35;
  }

  .collection-product-price {
    margin: auto 0 0;
    color: #B0A08C;
    font-weight: 700;
  }

  .empty-state {
    margin: 2rem 0 0;
    padding: 1rem;
    border: 1px dashed rgba(176, 160, 140, 0.5);
    border-radius: 10px;
    color: #666;
    text-align: center;
    background: #fffaf4;
  }

  @media (min-width: 900px) {
    .browse-layout {
      grid-template-columns: minmax(220px, 270px) 1fr;
    }
  }
</style>